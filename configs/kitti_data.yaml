# configs/kitti_data.yaml

dataset_name: KITTI

paths:
  raw_images: data/raw/images
  raw_labels: data/raw/labels
  processed_dir: data/processed

classes:
  - Car
  - Pedestrian
  - Cyclist

num_classes: 3

split:
  train: 0.70
  val: 0.15
  test: 0.15
  seed: 42

image:
  width: 1242
  height: 375
